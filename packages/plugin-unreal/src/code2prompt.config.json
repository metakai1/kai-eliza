{
  "include": ["**/*.ts"],  // Adjust based on the file types you want
  "exclude": [
    "**/test/**",      // Ignore all directories with "test" in their name
    "**/*example*",    // Ignore files containing "example" in their name
    "**/providers.ts", // Ignore the specific file providers.ts
    "**/actions.ts"    // Ignore the specific file actions.ts
  ],
  "options": {
    "recursive": true, // Recursively scan directories
    "ignoreCase": true, // Case-insensitive matches
    "output": "unreal_sourcecode.txt"
  }
}
